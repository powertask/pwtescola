
<div class='title_page'>
  <span> <%= t('contract.title') %> </span>
</div>

<p id="notice"><%= notice %></p>

<div class='list1'>
  <% unless @contracts.empty? %>
    <% @contracts.each do |contract| %>
      <li>
        <div class='strip' id='none'> <%= contract.id %> </div>

        <div class='indent'>
          <% if contract.cancel? %>
            <span class="label label-danger"><%= 'CANCELADO' %> </span> 
          <% elsif contract.open? %>
            <span class="label label-primary"><%= 'EM ABERTO' %> </span> 
          <% elsif contract.paid? %>
            <span class="label label-success"><%= 'PAGO' %> </span> 
          <% else %>
            <span><%= 'NAO DEFINIDO' %> </span> 
          <% end %>
          &nbsp;
          &bull;
          <%= link_to(contract.debtor.name, contract_path(contract)) %>
          &bull;
          <tt>
            <%= 'Data do Contrato: ' << contract.created_at.to_s_br %>
            &bull;
            <%= 'Responsável: ' << contract.user.name if contract.user %>
          </tt>
          <dt></dt>
        </div>
      </li>
    <% end %>
  <% else %>
    Devedor não tem contratos gerados.
  <% end %>
</div>

<div id='paginate'>
  <div class="apple_pagination">
    <%= will_paginate @contracts, :container => false  if @contracts.present? %>
  </div>
</div>
